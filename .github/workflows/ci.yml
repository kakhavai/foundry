name: CI

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  changes:
    runs-on: ubuntu-latest
    outputs:
      github-stats: ${{ steps.filter.outputs.github-stats }}
    steps:
      - uses: actions/checkout@v4
      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            github-stats:
              - 'services/github-stats/**'
              - 'helm/charts/github-stats/**'

  github-stats-lint-test:
    needs: changes
    if: needs.changes.outputs.github-stats == 'true'
    uses: ./.github/workflows/_python-lint-test.yml
    with:
      working-directory: services/github-stats

  github-stats-helm-lint:
    needs: changes
    if: needs.changes.outputs.github-stats == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: azure/setup-helm@v4
      - name: Lint chart
        run: helm lint helm/charts/github-stats
