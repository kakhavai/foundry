apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "github-stats.fullname" . }}-config
  labels:
    {{- include "github-stats.labels" . | nindent 4 }}
data:
  OTEL_EXPORTER_OTLP_ENDPOINT: {{ .Values.otel.endpoint | quote }}
  OTEL_SERVICE_NAME: "github-stats"
